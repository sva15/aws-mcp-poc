# MCP Server Dependencies — Cloud-Agnostic (no boto3)

# Official MCP Python SDK — provides FastMCP, Server, types, transports
mcp[cli]>=1.9.0

# ASGI server — runs the Starlette application
uvicorn>=0.34.0

# ASGI framework — mounts MCP + health check endpoints
starlette>=0.45.0

# HTTP client — for calling Tool Registry and Tool providers via HTTP
# Already a dependency of MCP SDK, listed explicitly for clarity
httpx>=0.28.0
