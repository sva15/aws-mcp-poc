# MCP Server Dependencies
# Official MCP Python SDK — provides FastMCP, Server, types, transports
mcp[cli]>=1.9.0

# ASGI server — runs the Starlette application
uvicorn>=0.34.0

# ASGI framework — used to mount MCP + health check endpoints
starlette>=0.45.0

# AWS SDK — for Lambda discovery and invocation
boto3>=1.35.0

# HTTP toolkit (required by MCP SDK's Streamable HTTP transport)
httpx>=0.28.0
